%h1 Items

%table.table.item_list
  %tr
    %th.description Item
    %th.days Days Left
    %th.done Finished?

  - @items.each do |item|
    %tr{ data: { id: item.id, value: item.archived.to_s } }
      %td.description= item.description
      %td.days= item.days_left
      %td.done 
        %p{ class: "#{center_button("dark_red", "sm")}" } Done!

%br

.center_button.thick-gray-border-top
  = render 'form'
