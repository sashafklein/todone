= form_for [@user, @item] do |f|
  - if @item.errors.any?
    - @item.errors.full_messages.each do |msg|
      %p= msg
      
  = f.label :description
  = f.text_field :description
  = f.submit 'Save'
